{
  "name": "smw200a-configurator",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Unofficial configurator for the Rohde & Schwarz SMW200A vector signal generator",
  "scripts": {
    "test": "node --test",
    "test:browser": "node tests/browser/run.mjs",
    "build": "node tools/build-standalone.mjs",
    "serve": "python3 -m http.server 8000"
  },
  "devDependencies": {
    "playwright": "^1.49.0"
  },
  "comment": "The site itself has no dependencies and no build step - open index.html from any static server. Playwright is only needed to run the browser suites, and the single-file build in tools/ has no dependencies either."
}
